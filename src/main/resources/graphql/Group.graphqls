extend type Mutation {
    createGroup(input: GroupInput!): GroupDto!
}

input GroupInput {
    groupName: String!
    groupDescription: String
    groupType: String
}
type GroupDto{
    groupId: String!
    groupName: String!
    description: String
    groupType: String
    active: Boolean
}